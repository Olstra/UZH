#include <stdio.h>


int INITIAL_STACK_SIZE = 5;	

typedef struct stackADT {
	int ∗elements;
	int size;
	int count;
} stack;

void initialize(stack *s){
	
 	stack->size = INITIAL_SZACK_SIZE;
/// 	// create array to save elements in
	// static int elements[INITIAL_STACK_SIZE];
	// stack->elements = elements; // point elements pointer to elements array
	// int count = 0;
}

int isEmpty(stack *s){}

int push(stack *s, int value){}

int pop(stack *s){}

int compareStack(stack *sA, stack *sB){}

int printStack(stack *s){}


int main(){

	initialize(stack);

	return 0;
}
