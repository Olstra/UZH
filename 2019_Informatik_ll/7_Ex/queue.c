#include <stdio.h>

void initialize(queue *q){}

int enqueue(queue *q, int value){}

int dequeue(queue *q){}

int compareQueue(queue *qA, queue *qB){}


int main(){

	initialize(stack);

	return 0;
}
